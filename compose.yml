services:
  api:
    build:
      context: ./api
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      NODE_ENV: development
      USE_HTTPS: "false"
      CLIENT_ORIGIN: http://localhost:5173
      MONGO_URI: ${MONGO_URI}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: 1h
